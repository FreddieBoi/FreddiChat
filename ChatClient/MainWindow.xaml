<Controls:MetroWindow x:Class="FreddiChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:FreddiChatClient="clr-namespace:FreddiChatClient"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:Infrastructure="clr-namespace:FreddiChatClient.Infrastructure"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        Title="FreddiChat" Height="475" 
        Width="750"
        ResizeMode="NoResize"
        BorderBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        WindowStartupLocation="CenterScreen"
        Loaded="WindowLoaded"
        Closing="WindowClosing">
    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Button Content="Tools" ToolTip="Tools">
                <i:Interaction.Behaviors>
                    <Infrastructure:ContextMenuOnLeftClickBehavior/>
                </i:Interaction.Behaviors>
                <Button.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Options..."/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
            <Button Content="Help" ToolTip="Help">
                <i:Interaction.Behaviors>
                    <Infrastructure:ContextMenuOnLeftClickBehavior/>
                </i:Interaction.Behaviors>
                <Button.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Wiki"/>
                        <Separator />
                        <MenuItem Header="Report a Bug"/>
                        <MenuItem Header="Technical Support"/>
                        <Separator />
                        <MenuItem Header="About FreddiChat"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>
    <Grid>
        <RichTextBox Height="312" HorizontalAlignment="Left" Margin="11,41,0,0" Name="chatTextBox" VerticalAlignment="Top" Width="586" IsReadOnly="True" IsEnabled="False" AcceptsReturn="False" VerticalScrollBarVisibility="Auto">
            <RichTextBox.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>
        <Button Height="23" HorizontalAlignment="Left" Margin="11,358,0,0" Name="boldButton" VerticalAlignment="Top" Width="23" ToolTip="Coming Soon..." IsEnabled="False">
            <Rectangle Width="13"
                Height="13"
                Fill="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_text_bold}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>
        <Button Height="23" HorizontalAlignment="Left" Margin="39,358,0,0" x:Name="italicButton" VerticalAlignment="Top" Width="23" ToolTip="Coming Soon..." IsEnabled="False">
            <Rectangle Width="13"
                Height="13"
                Fill="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_text_italic}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>
        <Button Height="23" HorizontalAlignment="Left" Margin="67,358,0,0" x:Name="underlineButton" VerticalAlignment="Top" Width="23" ToolTip="Coming Soon..." IsEnabled="False">
            <Rectangle Width="13"
                Height="13"
                Fill="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_text_underline}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>
        <Button Height="23" HorizontalAlignment="Left" Margin="95,358,0,0" x:Name="connectBold_Copy2" VerticalAlignment="Top" Width="23" ToolTip="Coming Soon..." IsEnabled="False">
            <Rectangle Width="13"
                Height="13"
                Fill="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_draw_brush_reflection}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>
        <ComboBox HorizontalAlignment="Left" Margin="123,358,0,0" VerticalAlignment="Top" Width="145" Height="24" IsEnabled="False" ToolTip="Coming Soon..."/>
        <ComboBox HorizontalAlignment="Left" Margin="273,358,0,0" VerticalAlignment="Top" Width="60" IsEnabled="False" ToolTip="Coming Soon..."/>
        <Button Height="23" HorizontalAlignment="Left" Margin="338,359,0,0" x:Name="emoticonButton" VerticalAlignment="Top" IsEnabled="False" Width="23" ToolTip="Coming Soon..." >
            <Rectangle Width="13"
                Height="13"
                Fill="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_smiley_grin}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>
        <TextBox Controls:TextboxHelper.Watermark="Username" Name="userNameTextBox" Height="23" HorizontalAlignment="Left" Margin="11,10,0,0" VerticalAlignment="Top" Width="130" TabIndex="0" />
        <TextBox Controls:TextboxHelper.Watermark="Server hostname or IP" Height="23" HorizontalAlignment="Left" Margin="146,10,0,0" Name="hostNameTextBox" VerticalAlignment="Top" Width="164" TabIndex="1" />
        <Button Height="23" HorizontalAlignment="Left" Margin="315,11,0,0" Name="connectButton" VerticalAlignment="Top" Width="23" IsDefault="True" Click="ConnectButtonClick" ToolTip="Connect" />
        <Button Content="Disconnect" Margin="484,12,162,0" x:Name="disconnectButton" IsEnabled="False" Background="LightGray" Style="{DynamicResource MetroFlatButton}" Width="96" Height="29" VerticalAlignment="Top" Click="DisconnectButtonClick" ToolTip="Disconnect" />
        <ListBox Margin="604,41,10,0" Name="userListBox" ItemsSource="{Binding}" Focusable="False" BorderBrush="Gray" BorderThickness="0.5" MouseDoubleClick="UserListBoxMouseDoubleClick" Height="394" VerticalAlignment="Top" />
        <TextBox Margin="10,388,0,0" Name="messageTextBox" IsEnabled="False" MaxLength="256" TabIndex="3" PreviewKeyDown="MessageTextBoxPreviewKeyDown" TextChanged="MessageTextBoxTextChanged" Height="48" VerticalAlignment="Top" HorizontalAlignment="Left" Width="509" />
        <Button Content="Send" Margin="510,388,0,0" x:Name="sendButton" IsEnabled="False" Click="SendButtonClick" Height="48" VerticalAlignment="Top" HorizontalAlignment="Left" Width="87" />
    </Grid>
</Controls:MetroWindow>
